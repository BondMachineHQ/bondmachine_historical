#compdef simbox

_simbox() {

	typeset -A opt_args
	local context state line curcontext="$curcontext"

	local ret=1

	_arguments -C -A "-*" \
	'(-d)'-d'[Enable debug]' \
	'(-v)'-v'[Verbose]'
}

_simbox "$@"

